# AGY 最高行为准则 (Highest Behavioral Principles)

> [!IMPORTANT]
> 本文件为整个系统与后续所有代码开发、UI 重构的**最高行为准则**。任何开发、功能迭代与界面设计必须无条件服从本准则。

---

## 核心法则：零预设 · 纯净白板 (Zero Presets & Blank Slate)

### 1. 绝对禁止硬编码/预置任何初始内容
- **禁止预设任何冒险故事**：应用在初次安装、启动或无数据状态下，严禁硬编码或内置任何预设的冒险剧本、探索章节、剧情故事。
- **禁止预设世界观**：严禁在代码中写死任何预设世界（例如“艾尔登幽邃迷雾”、“深空第十七前哨”、“雾都蒸汽诡案录”、“九霄长生遗卷”等假想世界）。
- **禁止预设角色与 NPC**：严禁内置任何预设角色卡、主角人设、伴随 NPC 档案或属性模板。
- **禁止预设行动选项与开场白**：向导或输入框内严禁预填具体的剧情描写、世界法则、开场对话或具体选项。

### 2. 必须呈现纯净的初始状态（白板状态 Blank Slate）
- **真正的白板 (True Blank Slate)**：所有数据必须源于**用户主动创建、自主编写**，或由用户明确下发指令后由 **AI 实时按需生成**。
- **优雅的空状态设计 (Elegant Empty States)**：
  - 当无冒险记录时：展示极简、雅致、富于设计感的空状态引导（如“尚未踏入任何世界，点击下方按钮开始初次探索”），配以新建按钮。
  - 当无世界观/角色时：展示空状态引导卡片与创建按钮，不填充任何示例假数据。
  - 创建向导与表单中：仅提供占位提示符（Hint Text，如“输入你的世界观名称...”、“描写开局发生的事件...”），字段初始值必须为空，严禁塞入预设文字。

### 3. 所有界面彻底重构原则
- 前端所有界面（大厅、向导、会话交互界面、世界观/角色资料库 Codex、提示词与世界设定、设置中心等）必须统一遵循现代 Material 3 极简设计系统，彻底消灭历史遗留的丑陋、杂乱、高耦合旧界面，且全域贯彻“纯净初始状态”。

---

## 统一组件规范：全域下拉选择组件 (`AppDropdown`)

> [!IMPORTANT]
> **全局强制约束**：凡是涉及单选、多选、标签定位、关系网属性、模型与参数配置等**任何下拉菜单交互**，**绝对禁止**使用 Flutter 原生未经美化的 `DropdownButton` 或 `DropdownButtonFormField`，必须无条件使用统一封装的现代 Material 3 组件 `AppDropdown`（或多选 `AppMultiSelectDropdown`）。

### 1. 原生 `DropdownButton` 的彻底禁用原因
- **遮挡原触发器**：原生 `DropdownButton` 会在原触发按钮正上方强制展开全屏层，彻底挡住原选项文案与卡片上下文。
- **生硬直角与视觉断层**：原生菜单默认采用直角黑框、无圆角、无呼吸边框、无微质感投影，与系统优雅的 Material 3 沉浸式风格格格不入。
- **缺乏交互反馈与指示**：原生组件缺乏项目悬停（Hover）过渡微动效，也没有对当前选中项的视觉主色着重与对勾（Checkmark）标识。

### 2. `AppDropdown` 核心形态与选用规则
`AppDropdown<T>` 位于 `lib/core/widgets/app_dropdown.dart`，针对不同场景提供专用构造器与视觉变体：

| 构造器 / 变体 | 适用场景 | 尺寸与视觉特性 |
| :--- | :--- | :--- |
| **`AppDropdown.compact`** | 角色卡身份定位、双人羁绊关系、表格内嵌标签、工具栏紧凑筛选 | 高度 32px，内敛圆角微边框（`AppRadius.sm`），自适应最小宽度（防文字挤压），不换行且与上下文行内自然对齐 |
| **`AppDropdown.form`** | 系统设置中心、大模型选择器、资料库属性编辑、长表单输入 | 高度 46px，圆角表单框（`AppRadius.lg`），支持 `label` 标头、`prefix` 前置图标、`subtitle` 说明副标题与错误态提示 |
| **`AppDropdown.tonal`** | 高频状态切换、强调型滤镜标签、模式选择 | 主色容器高亮胶囊（`primaryContainer`），椭圆全圆角（`AppRadius.full`） |
| **`AppMultiSelectDropdown`** | 批量导入筛选、多标签组合关联、多重角色/世界观勾选 | 支持集合 `Set<T>` 绑定，浮层保持常驻勾选，点击选项外侧优雅收起 |

### 3. 浮层菜单 (`OverlayMenu`) 视觉与弹出规范
- **默认向下弹出 (Default Downward Popup)**：所有下拉浮层**默认必须向下（Down）弹出展开**（`direction: AppDropdownDirection.down`）。菜单最大高度严格限制在屏幕底部安全区域之内（`min(menuMaxHeight, spaceBelow - 16.0)`）并支持流畅内嵌滚动，**严禁在有足够下方空间时向上翻折遮挡上方卡片与核心表单上下文**。仅在开发者显式指定或全屏极度拥挤且底部空间严重不足时才允许向上弹出。
- **统一视觉质感**：背景强制采用 `colorScheme.surfaceContainerHigh`，平滑圆角 `AppRadius.lg` (12px)，微透边框 `outlineVariant.withValues(alpha: 0.35)`，柔和全局弥散阴影（`blurRadius: 16`）。
- **项级高品质交互**：
  - 高度紧凑（36-40px），最大高度 280-320px 配合平滑滚动条，杜绝全屏突兀伸展。
  - 选中项自动高亮：浅色主色透明底（`primary.withValues(alpha: 0.12)`）+ 主色加粗字重 + 右侧对勾标记（`Icons.check_rounded`）。
  - 悬停（Hover）支持 `onSurface.withValues(alpha: 0.06)` 微反馈。
